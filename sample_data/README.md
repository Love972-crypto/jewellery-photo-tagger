# Sample Data

Place sample jewellery photos here for manual testing, or generate synthetic batches with:

```powershell
python tools/generate_synthetic_images.py --count 10 --output sample_data/synthetic_10
```

Synthetic files are useful for pipeline, duplicate, report, and ZIP testing. Real OCR accuracy should still be checked with real jewellery tag photos.

